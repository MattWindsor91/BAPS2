namespace URY.BAPS.Server.Reference.Config
{
    public enum Protocol
    {
        /// <summary>
        ///     Used to select the V2 (binary) protocol.
        /// </summary>
        BapsNetV2
    }
}